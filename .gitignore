# --- Node.js ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
package-lock.json
# Skip lock files only if you are building a library, but for a BOT it's better to keep it.
# However, if the user wants "nothing personal", sometimes lockfiles can contain local paths.
# I'll keep it for now as it's standard.

# --- Environment & Secrets ---
.env
.env.local
.env.*.local
*.pem
*.key
auth.json
config.json
# Excluding any potential local config that might contain tokens

# --- Local Data ---
data/
db.sqlite
*.log
.database/

# --- IDEs & Editors ---
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS Specific ---
.DS_Store
Thumbs.db
desktop.ini

# --- Agent specific ---
.gemini/
.agents/
.agent/
brain/
implementation_plan.md
walkthrough.md
task.md
